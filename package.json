{
  "name": "just-day-release",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend run dev --",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
